extends Area2D

func _on_CameraZone_body_entered(body):
	#print ("body: %s entered" % body.get_name())
	pass

func _on_CameraZone_body_exited(body):
	#print ("body: %s exited" % body.get_name())
	pass

